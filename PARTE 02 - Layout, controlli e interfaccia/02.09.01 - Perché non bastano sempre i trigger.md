# 02.09.01 - Perché non bastano sempre i trigger

## I trigger sono utili, ma non risolvono tutto

In WPF i trigger sono uno strumento molto importante. Permettono di reagire a certe condizioni e cambiare proprietà visive in modo dichiarativo.

Per esempio, con un trigger puoi dire:
- se il mouse passa sopra un pulsante, cambia lo sfondo;
- se un controllo è disabilitato, riduci l'opacità;
- se una proprietà assume un certo valore, modifica l'aspetto.

Questo approccio è ottimo in molti casi semplici.

Il problema nasce quando la UI non ha più solo piccoli cambiamenti locali, ma veri **stati visivi** con una logica più organizzata.

## Il limite mentale dei trigger

Il limite non è che i trigger “non funzionano”. Funzionano benissimo. Il punto è che, crescendo la complessità della UI, i trigger iniziano a diventare meno espressivi.

Per esempio, immagina un controllo che può essere:
- normale;
- con mouse sopra;
- premuto;
- disabilitato;
- selezionato;
- in errore;
- in caricamento.

Se cerchi di gestire tutto solo con trigger sparsi, rischi di ottenere:
- markup più lungo;
- stati che si sovrappongono;
- logica visiva meno chiara;
- difficoltà a capire quale stato stia prevalendo.

Qui comincia a emergere il valore del `VisualStateManager`.

## Trigger: ottimi per reazioni locali

I trigger restano perfetti quando:
- il cambiamento è piccolo;
- la logica è semplice;
- vuoi modificare poche proprietà;
- non stai modellando veri stati dell'interfaccia.

Esempio:

```xml
<Style TargetType="Button">
    <Setter Property="Background" Value="#2D6CDF"/>
    <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
            <Setter Property="Opacity" Value="0.9"/>
        </Trigger>
        <Trigger Property="IsEnabled" Value="False">
            <Setter Property="Opacity" Value="0.5"/>
        </Trigger>
    </Style.Triggers>
</Style>
```

Questo va benissimo. È leggibile e proporzionato al problema.

## Quando i trigger iniziano a diventare scomodi

Le difficoltà arrivano quando:
- gli stati sono numerosi;
- le transizioni tra stati contano;
- vuoi pensare in termini di “modalità” del controllo;
- vuoi una struttura più esplicita per l'aspetto visivo.

In quei casi il trigger reagisce, ma non organizza davvero.

## Il cambio di prospettiva

Con i trigger ragioni così:
“Se succede questa condizione, cambia questa proprietà”.

Con il `VisualStateManager` ragioni così:
“Questo controllo adesso si trova in questo stato visivo”.

È una differenza sottile, ma molto importante.

Nel primo caso reagisci a eventi o condizioni.
Nel secondo modelli esplicitamente gli stati della UI.

## Un piccolo esempio mentale

Immagina un pulsante con tre comportamenti:
- normale;
- hover;
- premuto.

Con trigger puoi cavartela.
Ma se vuoi anche:
- transizioni più controllate;
- uno stato disabilitato ben definito;
- una struttura visiva chiara e ampliabile,

allora comincia a essere più naturale ragionare per stati.

## Quando i trigger bastano davvero

Bastano quando:
- il controllo è semplice;
- la personalizzazione è leggera;
- gli stati sono pochi;
- non hai bisogno di una macchina degli stati visivi più esplicita.

## Quando conviene pensare al `VisualStateManager`

Conviene quando:
- il controllo ha più stati coerenti e distinti;
- vuoi un'organizzazione visiva più chiara;
- vuoi controllare meglio le transizioni;
- stai lavorando su template o controlli più ricchi.

## Errore tipico

L'errore classico è duplice:
- o usare i trigger per tutto, anche quando il markup sta già diventando confuso;
- o saltare troppo presto al `VisualStateManager` per casi che non lo richiedono.

La scelta giusta dipende dal livello di complessità reale.

## Regola pratica

Se devi fare poche reazioni visive semplici, i trigger sono ottimi.
Se stai modellando veri stati dell'interfaccia, il `VisualStateManager` inizia a diventare più espressivo e più ordinato.
