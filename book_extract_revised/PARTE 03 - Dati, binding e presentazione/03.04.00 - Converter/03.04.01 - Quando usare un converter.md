# 03.04.01 - Quando usare un converter

## Il problema che i converter risolvono davvero

Nel data binding ideale, la proprietà della source e la proprietà del target parlano già la stessa lingua.

Per esempio:
- una `string` va in `Text`;
- un `bool` va in `IsChecked`;
- una collezione va in `ItemsSource`.

Ma nella pratica reale succede molto spesso che il dato del modello non sia già nel formato o nel tipo giusto per la UI.

Esempi tipici:
- hai un `bool`, ma la UI vuole una `Visibility`;
- hai un numero, ma vuoi mostrarlo formattato in un certo modo;
- hai un valore che deve diventare un testo descrittivo;
- hai un enum che deve tradursi in un colore, un'icona o una stringa.

Qui entrano in gioco i converter.

## Che cosa fa un converter

Un converter prende un valore della source e lo trasforma in qualcosa che la proprietà target può usare meglio.

In altre parole:
non cambia il dato “vero” del modello,
ma cambia il modo in cui quel dato viene presentato o interpretato nel binding.

Questa distinzione è importantissima.

## Quando usarlo davvero

Usa un converter quando:
- il binding è corretto come concetto, ma i tipi o il formato non coincidono;
- la trasformazione è chiaramente di presentazione;
- vuoi evitare di sporcare il modello con logica solo UI;
- la conversione è riusabile in più punti.

## Esempi classici

### `bool` → `Visibility`
Molto comune per mostrare o nascondere un elemento.

### numero → testo formattato
Per esempio prezzi, percentuali, quantità.

### stato → stringa leggibile
Per esempio un enum `StatoDocumento` che deve diventare `"Bozza"`, `"Confermato"`, `"Annullato"`.

### valore → colore o brush
Molto usato in schermate con indicatori visivi.

## Quando non usarlo

Non tutto ciò che “trasforma” un valore deve diventare un converter.

Non usarlo quando:
- la logica appartiene chiaramente al dominio o al modello;
- la proprietà potrebbe essere esposta già pronta dal view model in modo più leggibile;
- la conversione è così specifica da vivere bene solo in un punto;
- stai cercando di fare troppa logica nel binding.

In particolare, se il valore che ti serve è una vera proprietà di presentazione stabile, a volte è più chiaro esporla direttamente nel view model.

## Un confronto utile

### Caso adatto a converter
Hai `bool IsInErrore` e vuoi controllare la `Visibility` di un messaggio.

### Caso meno adatto
Hai una logica complessa di business per calcolare uno stato finale del documento. Quella logica non dovrebbe stare in un converter.

## Perché i converter sono utili ma delicati

Sono utili perché rendono il binding molto flessibile.
Sono delicati perché, se abusati, trasformano lo XAML in un punto dove si nasconde troppa logica.

Per questo la regola giusta è:
usarli come adattatori di presentazione, non come sostituti del modello o del view model.

## Errore tipico

L'errore classico è usare un converter ogni volta che qualcosa non torna subito nel binding, anche quando sarebbe più chiaro:
- aggiungere una proprietà nel view model;
- rivedere il modello;
- usare una semplice formattazione;
- cambiare il controllo.

## Regola pratica

Usa un converter quando devi adattare un valore alla UI in modo chiaro, locale e riusabile.
Se stai mettendo dentro il converter logica di dominio o logica troppo articolata, probabilmente sei andato oltre il suo ruolo giusto.
